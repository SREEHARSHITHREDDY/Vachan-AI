# --- Trimmed for the 8-day demo scope ---
# psycopg2-binary and chromadb are DEFERRED, not forgotten:
# - Demo scope uses SQLite (Python's built-in sqlite3, no package needed),
#   per the scoped-down plan agreed for the 27th evaluation.
# - chromadb (semantic/vector search) isn't used by any module built so far
#   (Extraction Service, Lifecycle Tracker) — it's a later-stage dependency.
# Both were also the source of a real Python 3.14 build failure (pydantic-core
# and psycopg2-binary don't have 3.14-compatible wheels yet) — removing them
# sidesteps that entirely rather than fighting Python version compatibility.
# Add them back in Stage 3+ when the database/vector layer is actually built,
# at which point also pin a Python version (3.11 or 3.12) in a runtime file.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
groq==0.11.0
sqlalchemy==2.0.35
python-dotenv==1.0.1

# testing
pytest==8.3.3
pytest-cov==5.0.0
